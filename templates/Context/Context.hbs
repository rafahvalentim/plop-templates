import { createContext, useContext } from 'react';

interface {{pascalCase name}}Props {
  children: React.ReactNode;
}

interface {{pascalCase name}}Data {
  
}

const {{pascalCase name}}Context = createContext<{{pascalCase name}}Data>(
  {} as {{pascalCase name}}Data
);

const {{pascalCase name}}Provider = ({ children }: {{pascalCase name}}Props) => {
  return (
    <{{pascalCase name}}Context.Provider value={}>
        { children }
    </{{pascalCase name}}Context.Provider>
  );
};

function use{{pascalCase name}}(): {{pascalCase name}}Data {
  const context = useContext({{pascalCase name}}Context);

  if (!context) {
    throw new Error('useTheme must be used within a {{pascalCase name}}');
  }
  return context;
}
export { {{pascalCase name}}Provider, use{{pascalCase name}} };
